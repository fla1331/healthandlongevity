<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Seu Título</title>
  <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>
  {% include "header.njk" %}

  <main>
    {{ content | safe }}
  </main>

  {% set language_slug = post._embedded['wp:term'][0][0].slug %}
  {% if language_slug %}
    {% include "footer_" + language_slug + ".njk" %}
  {% else %}
    {% include "footer_pt.njk" %}
  {% endif %}
</body>
</html>